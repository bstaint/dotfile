augroup Plugged
    au!
    au FileType leaderf setlocal nonumber norelativenumber
augroup END

silent! if plug#begin($VIMFILES.'/plugged')
"{{{ buildin
    let g:phpcomplete_search_tags_for_variables = 1
"}}}

Plug 'pacha/vem-dark'

call plug#end()
endif
" vim:foldmethod=marker:
